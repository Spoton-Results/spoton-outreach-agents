[build]
builder = "NIXPACKS"

[deploy]
# No default startCommand — each Railway service sets its own start command as a service-level override.
# Having a startCommand here caused Railway to run push-to-instantly.js on every ON_FAILURE restart
# regardless of which service actually crashed. Removed to prevent that cascade.
restartPolicyType = "NEVER"
